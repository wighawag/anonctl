# compiled binaries (match the command dir names, produced by a bare `go build`)
/anonctl
/anonctl-shim

# goreleaser build output
/dist/

# fontconfig cache written by media/build.sh (vendored-font lookup, not an asset)
/media/.fc/
