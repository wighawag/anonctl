# The `verify` assertion names and `--json` report contract

Status: accepted

## Context

`anonctl verify` is the trust anchor and the signature ongoing verb: it PROVES an account is anonymized rather than assuming it, and is meant to be re-run after setup, a reboot, and any Tor/kernel/nftables change. Story 18 requires it to emit named assertions, exit non-zero on any failure, and support `--json` so CI/automation can gate on it (mirroring netcage's `verify`). The assertion NAMES and the JSON SHAPE are consumed by machines (a CI gate keys on a name; a sibling tool may read the report), so they are a CONTRACT, not incidental output. This ADR records the shape and the names so a later change is a deliberate, versioned decision rather than an accidental break.

## Decisions

- **The assertion set is a fixed, named vocabulary (kebab-case), declared once as constants, never spelled inline.** The names are `anonymized-exit`, `dns-remote`, `leak-drop-v4`, `leak-drop-v6`, `bypass-loopback-closure`, `bypass-endpoint-closure`, `icmp-drop`, `non-tcp-udp-drop`, `no-uid-transition-egress`, `split-tunnel-tight`, `lan-exemption-not-a-dns-hole`. kebab-case mirrors netcage's assertion names. `leak-drop-v4` and `leak-drop-v6` are SEPARATE names (not one `leak-drop`) so a v6 bypass of v4 rules can never hide behind a single line: each family reports independently. The two bypass closures map one-to-one onto the ruleset's two closures (ADR 0002): closure (a) non-shim-loopback drop, closure (b) direct-endpoint drop.

- **`icmp-drop` and `non-tcp-udp-drop` (added additively; schemaVersion unchanged) PROVE the ICMP and non-53-UDP leak classes are closed rather than assumed (Tails leak-catalogue rows 4 and 5).** Both classes already fall through to the anon UID's `policy drop` in the shipped ruleset (there is no explicit anon ICMP/UDP accept, so both hit the default DROP; confirmed against `internal/nftables`), so these add NO ruleset rule by default; they add the two verify assertions that prove the drop. `icmp-drop`: an ICMP echo (`ping`) from the anon UID to an off-box address is DROPPED (no ICMP packet carrying the real source IP leaves). `non-tcp-udp-drop`: raw non-53 UDP from the anon UID is DROPPED, specifically INCLUDING UDP/443 (QUIC / HTTP-3), which SOCKS (TCP-only) cannot relay. Two resolved caveats ride with them, recorded in the README threat model, NOT as extra assertions: (a) anonctl does NOT tune PMTU / set `net.ipv4.tcp_mtu_probing` (Tails does because it drops ALL ICMP OS-wide; anonctl drops ICMP for ONE UID only, and the anon UID's forced TCP rides the shim to a SOCKS proxy, so a global sysctl would be a surprising out-of-scope mutation); (b) a real client is EXPECTED to degrade UDP/443 to TCP rather than leak, which is client behaviour, not something an integration test can observe, so the provable claim is the drop. Adding both names is ADDITIVE (two new names + two new checks), so `schemaVersion` stays at 1: a pinned consumer keeps working and simply sees two more named assertions. The live probes run in the normal binary exactly like the leak-drop probes (they need root + setpriv + a provisioned host at runtime, failing loud when they cannot run).

- **`no-uid-transition-egress` (added additively; schemaVersion unchanged) is a BEST-EFFORT, explicitly NON-EXHAUSTIVE proof that the concretely enumerable UID-transition escape vectors do not leak (Tails leak-catalogue row 7).** anonctl forces egress by `meta skuid <anonUID>`, so a socket owned by a DIFFERENT uid (a setuid binary, `sudo`, a triggerable daemon) does not match `skuid == anonUID` and egresses in the clear. This assertion probes the CONCRETELY ENUMERABLE vectors the hand-audited finding (`work/notes/findings/uid-transition-escape-surface.md`) lists, NOT a guessed set: (a) the anon account has no sudo path (`sudo -l -U <account>`, the same probe provision's `sudoAllowed` reads), and (b) the small documented set of setuid "run a command as another uid" network wrappers present on the box (pkexec, mullvad-exclude) do not hand the account a process whose euid is a non-anon, non-shim uid. It passes IFF at least one vector was checked AND none escaped. Crucially it is reported HONESTLY as best-effort and NOT exhaustive: verify cannot enumerate every daemon on every host, so its detail string states plainly that the pass covers only the CHECKED vectors and that an arbitrary triggerable daemon on a busy host may still escape (only netns-strength confinement closes that class; that is netcage, a deliberate non-goal here). A false total-guarantee here would be worse than an honest partial one, so the non-exhaustive framing is load-bearing, not decoration. Adding this name is ADDITIVE (a new name + a new check), so `schemaVersion` stays at 1: a pinned consumer keeps working and simply sees one more named assertion. The live probes run in the normal binary exactly like the other live assertions (they need root + setpriv + a provisioned account at runtime, failing loud when they cannot run).

- **`lan-exemption-not-a-dns-hole` (added additively; schemaVersion unchanged) proves the LAN exemption never carries clear DNS (Tails leak-catalogue row 2).** With a LAN exemption active, clear DNS (tcp AND udp 53) to the exempted host must NOT egress directly to the LAN resolver (a `@192.168.x.x` clear-DNS query can reveal the local network's public IP, a deanonymization vector). It sits alongside `split-tunnel-tight` in the exemption-only group (both run only when an exemption is active) and is enforced structurally: `internal/lanexempt.Parse` rejects an explicit `:53`, and `internal/nftables` excludes 53 from a port-omitted (all-TCP) exemption (`tcp dport != 53`) so 53 stays redirected to the shim. Adding this assertion is an ADDITIVE change (a new name + a new check), so `schemaVersion` stays at 1: a pinned consumer keeps working and simply sees one more named assertion. The live probe reads the black-hole/counter signal the DNS subtlety requires (a transparent redirect means a naive `dig` still answers), never a "direct dig must fail" expectation.

- **`--json` is a versioned envelope carrying a derived top-level `ok`.** The wire shape is `{schemaVersion, ok, account, endpoint, assertions:[{name, ok, detail, error}]}`. `schemaVersion` starts at 1 and evolves ADDITIVELY only (new optional fields keep a pinned consumer working; a breaking change bumps it). The top-level `ok` is the derived greenness (`== Report.Ok()`), so a consumer gates on ONE boolean without re-walking the array. The wire type (`jsonReport`) is DISTINCT from the in-memory `Report`, so the contract is explicit and stable rather than whatever the struct happens to look like.

- **A report is a pass IFF every assertion passed AND at least one ran; an empty report is NOT a pass and exits non-zero.** "Nothing was asserted" is never proof of anonymization. A probe that ERRORED counts as a failure (a check that could not run is not a pass), and its error is flattened to a string in the JSON (`error`), never a Go error object.

- **The `endpoint` in the report is the credential-free socks5h URL (`endpoint.URL()`), never a `user:pass@` form.** A shared or logged verify report must never leak a secret; the per-account isolation username is derived at dial time, not embedded (consistent with ADR 0001's credential-free-at-rest stance).

- **The pure assertion/render/exit logic is split from the live probes; the live probes run in EVERY build (they are runtime behaviour, not tests).** The pure decisions (the `*Assertion` functions, `Report`, `Run`, `RunVerify`) are unit-tested EVERYWHERE against the socks5h fixture with no root and no real Tor; the live probes that stand up real connections AS the anon UID (setpriv + nft + curl + ping + the installed `anonctl-shim -probe` dialer + a live endpoint) live in `checks_live.go` / `probes_live.go` and are compiled into the normal binary. Probing needs root at runtime exactly as `add`/`rm` do, so it is gated at RUNTIME, not by a build tag: a missing tool or an un-runnable probe is a LOUD failing assertion naming what it needed, and `verify` exits non-zero. The DEFAULT binary therefore both RUNS verification and cannot silently "pass": it never reports green unless it actually proved green, and never green merely because it could not probe. **Correction (this reverses the original decision):** the `integration` build tag was originally (mis)used to gate the live PRODUCT probes too, which made `go install` / goreleaser ship a binary whose `verify` always returned a `live-verify-available` fail stub and whose `use` always refused, i.e. the shipped tool was dead on arrival. The tag now gates ONLY the slow/privileged `*_test.go` files (so `go test ./...` still skips them and `go test -tags integration ./...` still runs them); the product's runtime probing is never behind it.

## Consequences

The account's endpoint host:port and the shim's relay/DNS ports are `LiveParams` inputs the runtime command discovers from the provisioned account. The normal-binary `verify` runs the real probes against that account (the `integration` test harness supplies the live params directly for its throwaway accounts). Adding a new assertion is additive (append a name constant + a check); removing or renaming one, or changing the JSON envelope's existing fields, is a breaking change that bumps `schemaVersion`.
